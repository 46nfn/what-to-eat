# 生产环境 - Vite 构建时自动注入 import.meta.env.VITE_API_BASE
#
# 部署流程：
#   1. Fly.io 部署后端 → 获得 https://你的app名.fly.dev
#   2. 将下方地址改为实际 Fly.io URL（不要带 /api 后缀）
#   3. npm run build → 部署 dist/ 到 Vercel

VITE_API_BASE=__FLY_IO_URL__
